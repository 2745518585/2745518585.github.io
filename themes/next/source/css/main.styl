// CSS Style Guide: https://codeguide.co/#css


$scheme = hexo-config('scheme') ? hexo-config('scheme') : 'Muse';


// Variables Layer
// --------------------------------------------------
@import '_variables/base';
@import '_variables/' + $scheme;
for $inject_variable in hexo-config('injects.variable')
  @import $inject_variable;

// Mixins Layer
// --------------------------------------------------
@import '_mixins';
for $inject_mixin in hexo-config('injects.mixin')
  @import $inject_mixin;

// Dark mode colors
// --------------------------------------------------
@import '_colors';

// Common Layer
// --------------------------------------------------

// Scaffolding
@import '_common/scaffolding';

// Layout
@import '_common/outline';

// Components
@import '_common/components';


// Schemes Layer
// --------------------------------------------------
@import '_schemes/' + $scheme;


// Custom Layer
// --------------------------------------------------
for $inject_style in hexo-config('injects.style')
  @import $inject_style;

.fa-zhihusingle {
  background: url("/icons/zhihusingle.svg");
  background-repeat: no-repeat;
  height: 1rem;
  transform: translate(0, 10%);
  opacity: 0.8;
}
.fa-codeforces {
  background: url("/icons/codeforces.svg");
  background-repeat: no-repeat;
  height: 1rem;
  transform: translate(0, 10%);
  opacity: 0.8;
}
.fa-atcoder {
  background: url("/icons/atcoder.svg");
  background-size: cover;
  height: 1rem;
  transform: translate(0, 20%);
  opacity: 0.8;
}
.fa-luogu {
  background: url("/icons/luogu.svg");
  background-repeat: no-repeat;
  height: 1rem;
  transform: translate(0, 15%);
  opacity: 0.8;
}
.fa-libreoj {
  background: url("/icons/libreoj.svg");
  background-repeat: no-repeat;
  height: 1rem;
  transform: translate(0, 15%);
  opacity: 0.8;
}